--- sirius.org/src/potential/potential.hpp	2025-06-23 15:36:56.000000000 +0900
+++ sirius/src/potential/potential.hpp	2025-06-23 15:37:57.000000000 +0900
@@ -671,7 +671,7 @@
         }
         /* compute contribution from the core */
         double ecore{0};
-        #pragma omp parallel for reduction(+:ecore)
+        //#pragma omp parallel for reduction(+:ecore)
         for (auto it : unit_cell_.spl_num_paw_atoms()) {
             auto ia = unit_cell_.paw_atom_index(it.i);
 
@@ -706,7 +706,7 @@
     PAW_one_elec_energy(Density const& density__) const
     {
         double e{0};
-        #pragma omp parallel for reduction(+:e)
+        //#pragma omp parallel for reduction(+:e)
         for (auto it : unit_cell_.spl_num_paw_atoms()) {
             auto ia = unit_cell_.paw_atom_index(it.i);
             auto dm = density__.density_matrix_aux(atom_index_t::global(ia));

--- sirius.org/src/unit_cell/atom.hpp	2025-06-24 14:22:30.000000000 +0900
+++ sirius/src/unit_cell/atom.hpp	2025-06-24 14:20:55.000000000 +0900
@@ -29,6 +29,7 @@
 #include "core/profiler.hpp"
 #include "atom_symmetry_class.hpp"
 #include "function3d/spheric_function.hpp"
+#include <numeric>

 namespace sirius {

@@ -461,23 +462,48 @@
         std::complex<double> res{0};
         switch (sblock) {
             case spin_block_t::nm: {
-                res = std::transform_reduce(gnt__.begin(), gnt__.end(), std::complex<double>{0}, std::plus{}, nm);
+                //res = std::transform_reduce(gnt__.begin(), gnt__.end(), std::complex<double>{0}, std::plus{}, nm);
+                res = std::complex<double>{0};
+                for (auto& x : gnt__) {
+                    res += nm(x);
+                }
+
                 break;
             }
             case spin_block_t::uu: {
-                res = std::transform_reduce(gnt__.begin(), gnt__.end(), std::complex<double>{0}, std::plus{}, uu);
+                //res = std::transform_reduce(gnt__.begin(), gnt__.end(), std::complex<double>{0}, std::plus{}, uu);
+                res = std::complex<double>{0};
+                for (auto& x : gnt__) {
+                    res += uu(x);
+                }
+
                 break;
             }
             case spin_block_t::dd: {
-                res = std::transform_reduce(gnt__.begin(), gnt__.end(), std::complex<double>{0}, std::plus{}, dd);
+                //res = std::transform_reduce(gnt__.begin(), gnt__.end(), std::complex<double>{0}, std::plus{}, dd);
+                res = std::complex<double>{0};
+                for (auto& x : gnt__) {
+                    res += dd(x);
+                }
+
                 break;
             }
             case spin_block_t::ud: {
-                res = std::transform_reduce(gnt__.begin(), gnt__.end(), std::complex<double>{0}, std::plus{}, ud);
+                //res = std::transform_reduce(gnt__.begin(), gnt__.end(), std::complex<double>{0}, std::plus{}, ud);
+                res = std::complex<double>{0};
+                for (auto& x : gnt__) {
+                    res += ud(x);
+                }
+
                 break;
             }
             case spin_block_t::du: {
-                res = std::transform_reduce(gnt__.begin(), gnt__.end(), std::complex<double>{0}, std::plus{}, du);
+                //res = std::transform_reduce(gnt__.begin(), gnt__.end(), std::complex<double>{0}, std::plus{}, du);
+                res = std::complex<double>{0};
+                for (auto& x : gnt__) {
+                    res += du(x);
+                }
+
                 break;
             }
             default: {

--- src.org/density/density.cpp	2025-06-24 14:53:39.000000000 +0900
+++ src/density/density.cpp	2025-06-24 14:58:08.000000000 +0900
@@ -556,7 +556,7 @@

     PROFILE("sirius::Density::generate_paw_density");

-    #pragma omp parallel for
+    //#pragma omp parallel for
     for (auto it : unit_cell_.spl_num_paw_atoms()) {
         generate_paw_density(it.li);
     }
@@ -778,11 +778,11 @@
     auto one = la::constant<std::complex<double>>::one();

     /* add |psi_j> n_j <psi_j| to density matrix */
-    #pragma omp parallel
+//    #pragma omp parallel
     {
         mdarray<std::complex<double>, 3> wf1({uc.max_mt_basis_size(), ctx__.num_bands(), ctx__.num_spins()});
         mdarray<std::complex<double>, 3> wf2({uc.max_mt_basis_size(), ctx__.num_bands(), ctx__.num_spins()});
-        #pragma omp for
+//        #pragma omp for
         for (auto it : kp__.spinor_wave_functions().spl_num_atoms()) {
             int ia            = it.i;
             int mt_basis_size = uc.atom(ia).type().mt_basis_size();

--- src.org/hamiltonian/hamiltonian_k.cpp	2025-06-24 14:53:39.000000000 +0900
+++ src/hamiltonian/hamiltonian_k.cpp	2025-06-24 15:00:31.000000000 +0900
@@ -858,7 +858,7 @@

     /* apply APW-lo part of Hamiltonian to lo- part of wave-functions */
     auto apply_hmt_apw_lo = [this, &ctx, &phi__, la, mem, &b__, &spl_atoms](wf::Wave_functions_mt<T>& h_apw_lo__) {
-        #pragma omp parallel for
+        //#pragma omp parallel for
         for (auto it : spl_atoms) {
             int tid    = omp_get_thread_num();
             int ia     = it.i;
@@ -886,7 +886,7 @@
     auto apply_omt_apw_lo = [this, &ctx, &phi__, &b__, &spl_atoms](wf::Wave_functions_mt<T>& o_apw_lo__) {
         o_apw_lo__.zero(memory_t::host, wf::spin_index(0), wf::band_range(0, b__.size()));

-        #pragma omp parallel for
+        //#pragma omp parallel for
         for (auto it : spl_atoms) {
             int ia     = it.i;
             auto& atom = ctx.unit_cell().atom(ia);
@@ -916,7 +916,7 @@

     auto appy_hmt_lo_lo = [this, &ctx, &phi__, la, mem, &b__, &spl_atoms](wf::Wave_functions<T>& hphi__) {
         /* lo-lo contribution */
-        #pragma omp parallel for
+        //#pragma omp parallel for
         for (auto it : spl_atoms) {
             int tid    = omp_get_thread_num();
             auto ia    = it.i;
@@ -939,7 +939,7 @@

     auto appy_omt_lo_lo = [this, &ctx, &phi__, &b__, &spl_atoms](wf::Wave_functions<T>& ophi__) {
         /* lo-lo contribution */
-        #pragma omp parallel for
+        //#pragma omp parallel for
         for (auto it : spl_atoms) {
             auto ia    = it.i;
             auto& atom = ctx.unit_cell().atom(ia);
@@ -973,7 +973,7 @@

     auto appy_hmt_apw_apw = [this, &ctx, la, mem, &b__](int atom_begin__, wf::Wave_functions_mt<T> const& alm_phi__,
                                                         wf::Wave_functions_mt<T>& halm_phi__) {
-        #pragma omp parallel for
+        //#pragma omp parallel for
         for (auto it : alm_phi__.spl_num_atoms()) {
             int tid    = omp_get_thread_num();
             int ia     = atom_begin__ + it.i;
@@ -996,7 +996,7 @@

     auto apply_hmt_lo_apw = [this, &ctx, la, mem, &b__, &spl_atoms](wf::Wave_functions_mt<T> const& alm_phi__,
                                                                     wf::Wave_functions<T>& hphi__) {
-        #pragma omp parallel for
+        //#pragma omp parallel for
         for (auto it : spl_atoms) {
             int tid    = omp_get_thread_num();
             int ia     = it.i;
@@ -1021,7 +1021,7 @@

     auto apply_omt_lo_apw = [this, &ctx, mem, &b__, &spl_atoms](wf::Wave_functions_mt<T> const& alm_phi__,
                                                                 wf::Wave_functions<T>& ophi__) {
-        #pragma omp parallel for
+        //#pragma omp parallel for
         for (auto it : spl_atoms) {
             int ia     = it.i;
             auto& atom = ctx.unit_cell().atom(ia);
